[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "python -m app.ingestion"
startCommand = "uvicorn app.api:app --app-dir BACKEND --host 0.0.0.0 --port $PORT"
restartPolicy = "ON_FAILURE"
restartPolicyMaxRetries = 10